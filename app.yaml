runtime: nodejs22
env: standard
instance_class: F1
automatic_scaling:
  min_instances: 1
  max_instances: 10

env_variables:
  NODE_ENV: production
  PORT: 8080

handlers:
  - url: /.*
    script: auto
